name: CI

on: [push]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v1
            - name: Use Node.js 14
              uses: actions/setup-node@v1
              with:
                  node-version: '14'
            - name: install deps
              run: npm ci
            - name: verify
              run: npm run verify
            - name: build
              run: npm run build
